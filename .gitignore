# 编译产物 (make all 可重新生成, 单个约 1.2MB)
# 用模式而不是逐个列文件名, 这样新增 benchmark 不会漏
Latency/0*_*
!Latency/0*_*.cu
!Latency/0*_*.md

# SASS dump (make sass 生成, 每份数 MB)
sass-*.txt

# 临时/编辑器文件
*.o
*.cubin
*.ptx
*~
.DS_Store
